package cn.zju.leetcode;

public class code18_1473____________ {
    /*public int minCost(int[] houses, int[][] cost, int m, int n, int target) {
        if (houses[m - 1] != 0) {
            return process(houses, cost,m - 1, houses[m - 1], target);
        } else {
            int min = Integer.MAX_VALUE;
            for (int color = 1; color <= n; color++) {
                min = Math.min(min, process(houses, cost, m - 1, color, target));
            }
            return min;
        }
    }
    public int process(int[] houses, int[][] cost, int i, int j, int k) {
        if (k <= 0) {
            return Integer.MAX_VALUE;
        }
        if (houses[i] != 0) {
            if (houses[i] != j) {
                return Integer.MAX_VALUE;
            } else {

            }
        }
    }*/
}
